const { describe, it, beforeEach } = require('mocha')
const { expect } = require('chai')
const PostObject = require('../../build/post.helpers')

describe('build post helpers', () => {
    describe('createPostObject()', () => {
        it('should return a post object')
    })
})
