import { describe, it } from 'mocha'

describe('[server] posts helpers', () => {
    it('should read posts in assets directory')
    it('should retrieve markdown metadata')
    it('should convert markdown to html')
    it('should return an array of converted post objects')
})
